# -*- encoding : utf-8 -*-
# ActionController::Base.logger = ActiveSupport::BufferedLogger.new("#{Rails.root}/log/#{Rails.env}.controller.log")
# ActiveRecord::Base.logger = ActiveSupport::BufferedLogger.new("#{Rails.root}/log/#{Rails.env}.database.log")
# 
#  class Logger
#    def format_message(severity, timestamp, progname, msg)
#      "#{timestamp} (#{$$}) #{msg}\n"
#    end
#  end
