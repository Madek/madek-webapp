%label.term
  %input{:type => "checkbox", :value => "${id}"}
  %span.text
    {{if value}}
    ${value}
    {{else}}
    %span.unknown
      = "unbekannt"
    {{/if}}
  {{if count}}
  %span.count ${(count>9999) ? ">9999" : count} 
  {{/if}}
  
