.modal.hide.fade.ui-modal.narrow{:role => "dialog", :tabindex => "-1"}
  %form
    .ui-modal-head
      %a.ui-modal-close{"aria-hidden" => "true", "data-dismiss" => "modal", :type => "button", :title => "Close", :class => "weak"}
      %h3.title-l ${_jed('Create set')}
    .ui-modal-body
      .form-body
        .ui-form-group.rowed
          %label.form-label
            ${_jed('Title')}
          .form-item
            %input.block{:name => :title, :autofocus => :autofocus, :type => "text", :autocomplete => "off"}
    .ui-modal-footer
      .ui-actions
        %a.link.weak{"aria-hidden" => "true", "data-dismiss" => "modal", :class => "weak link"} ${_jed('Cancel')}
        %button.primary-button{:type => :submit} ${_jed('Create set')}
