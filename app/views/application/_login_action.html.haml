- primary = (!!defined?(primary) and !primary.nil?) ? primary : true
- if current_user.is_guest?
  .dropdown.ui-dropdown.stick-right
    %a.dropdown-toggle{:href => root_path, :class => primary ? "primary-button" : "button"}
      %i.icon-man_silhouette
      = _("Log in")

