:ruby
  mods = []
  # HACK: toggle box via prop…
  with_box = (true if params[:action] != 'dashboard')
  mods << ['rounded-right', { bordered: false }] if (params[:action] != 'dashboard')
  mods << 'unpaginated' if (local_assigns[:show_pagination_nav] == false)
  fallback = section[:is_empty?] ? true : false

= react('Deco.MediaResourcesBox',
    get: @get.send(section[:id]),
    mods: mods,
    withBox: with_box,
    fallback: fallback,
    initial: { show_filter: false })
