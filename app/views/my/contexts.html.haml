.app.view-my-contexts

  = render "application/header"

  .app-body

    .app-body-title
      .ui-body-title
        .ui-body-title-label
          %h1.title-xl
            = _("Vokabular")

    .app-body-ui-container
      .bordered.ui-container.midtone.rounded.table

        = render :partial => "application/sidebar/my_archive"

        .app-body-content.table-cell.table-substance.ui-container
          
          = render :partial => "contexts/wrapper", 
                     :locals => {:contexts => @contexts, 
                                 :title => _("Vokabular")}

  .app-footer-push
= render :partial => "application/footer"
