<%
  content_for(:page_header) do
    component('page-header',
              # text: 'New Workgroup',
              actions: [
                component('button.primary', link: new_my_workflow_path,
                                            text: 'New workflow')
              ])
  end
%>

<%=
react('Views.My.Workflows', get: @get.section_content)
%>
