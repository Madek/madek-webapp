:ruby
  text ||= nil
  icon ||= nil
  actions ||= nil

.ui-body-title

  .ui-body-title-label
    %h1.title-xl
      = component("icon.#{icon}")
      = text

  - if actions.is_a?(Enumerable)
    .ui-body-title-actions
      - actions.each do |btn|
        = btn
