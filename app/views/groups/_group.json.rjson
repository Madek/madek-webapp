h = {
  type: group.type,
  id: group.id,
  name: group.name
}

if users ||= nil
  h[:users] = users.map do |user|
    {
      id: user.id,
      login: user.login,
      lastname: user.person.lastname,
      firstname: user.person.firstname
    }
  end
end

h
