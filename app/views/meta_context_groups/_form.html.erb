<%= form_for(@meta_context_group) do |f| %>
  <% if @meta_context_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meta_context_group.errors.count, "error") %> prohibited this meta_context_group from being saved:</h2>

      <ul>
      <% @meta_context_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
