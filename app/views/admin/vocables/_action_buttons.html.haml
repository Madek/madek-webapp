.btn-group
  = link_to edit_admin_vocabulary_vocable_path(@vocabulary, vocable), class: "btn btn-info #{size}" do
    %i.glyphicon.glyphicon-edit
    Edit
  = link_to admin_vocabulary_vocable_path(@vocabulary, vocable), method: :delete, data: {   |
    confirm: 'The vocable will be deleted. Do you want to proceed?' }, |
    class: "btn btn-danger #{size}" do
    %i.glyphicon.glyphicon-trash
    Delete
