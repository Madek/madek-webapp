<h1>Editing permission_preset</h1>

<%= semantic_form_for @permission_preset, url: admin_permission_preset_path(@permission_preset) do |f| %>
  <%= f.inputs %>
  <%= f.buttons %>
<% end %>

<%= link_to 'Show', admin_permission_preset_path(@permission_preset) %> |
<%= link_to 'Back', admin_permission_presets_path %>
