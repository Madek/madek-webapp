%tr
  %td= preview.content_type
  %td= preview.thumbnail
  %td= preview.created_at
  %td
    .btn-group
      = link_to admin_preview_path(preview), class: 'btn btn-default btn-sm' do
        %span.glyphicon.glyphicon-eye-open
      = link_to admin_preview_path(preview), method: :delete, class: 'btn btn-danger btn-sm',
        data: { confirm: 'Preview will be deleted. Do you want to proceed?' } do
        %span.glyphicon.glyphicon-trash
        Delete
