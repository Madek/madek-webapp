= form_for [:admin, @api_client], html: { class: 'form-horizontal', role: 'form' } do |f|

  %fieldset
    .form-group
      = f.label :login, class: 'control-label col-xs-2'
      .col-xs-4
        = f.text_field :login, class: 'form-control'

    .form-group
      = f.label :user_id, 'User', class: 'control-label col-xs-2'
      .col-xs-4
        = f.select :user_id,
            options_for_select(User.all.collect { |u| ["#{u.person} [#{u.login}]", u.id]}, f.object.user_id),
            {}, class: 'form-control'

    .form-group
      = f.label :description, class: 'control-label col-xs-2'
      .col-xs-8
        = f.text_area :description, class: 'form-control', rows: 5

    .form-group
      .col-xs-offset-2.col-xs-4
        = button_tag type: 'submit', class: 'btn btn-success' do
          %span.glyphicon.glyphicon-ok
          Save
        = link_to 'Go back', @api_client.persisted? ? admin_api_client_path(@api_client) : admin_api_clients_path, class: 'btn btn-link'
