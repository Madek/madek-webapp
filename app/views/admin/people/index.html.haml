%table
  %tr
    %td{:colspan => 5, :style => "text-align: right; white-space: nowrap;"}
      = link_to _("New Person"), new_admin_person_path, :class => "buttons", :remote => true, :"data-type" => "html"

%table.datatable
  %thead
    %tr
      %th ID
      %th Firstname
      %th Lastname 
      %th MetaData
      %th Pseudonym
      %th User
      %th
      %th

  %tbody
    = render :partial => "show", :collection => @people, :as => :person
