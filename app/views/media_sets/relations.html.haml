:ruby
  @self = @media_set
  @is_set = true
  @description = "Das ausgewählte Set ist mit anderen Sets verknüpft. Diese Zusammenhänge wurden aktiv festgelegt als übergeordnet, benachbart oder untergeordnet. \nSie sehen hier sowohl eigene Sets, als auch solche, die andere Nutzer/innen mit Ihnen teilen."

- content_for :content do
  = render "media_resources/relations"

= render "media_sets/layout"
