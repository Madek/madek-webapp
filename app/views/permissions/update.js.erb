<% if (@permission.subject.id == current_user.id) && @permission.resource.is_a?(MediaEntry) %>
  var new_download_panel = "<%=escape_javascript(render(:partial => '/media_entries/download', :object => @permission.resource, :as => :media_entry))%>";
  $("#download-panel").html(new_download_panel);
<% end %>
