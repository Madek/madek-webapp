%h1 New Group

= form_for @group, url: app_admin_groups_path, method: "POST", html: {class: "form-horizontal"} do |f|

  %fieldset

    .control-group
      %label.control-label Name
      .controls
        = f.text_field :name


  = button_tag "Submit", type: 'submit', class: "btn btn-success"

