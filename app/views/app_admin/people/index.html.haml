%h1.text-center
  %i.icon-user
  People

.row

  %div.span3

    %h3 
      %i.icon-filter
      Filters

    = form_tag app_admin_people_path, method: 'GET' do

      %label 
        = text_field nil, :fuzzy_search, class: "search-query", value: params[:fuzzy_search] 

      %label.checkbox 
        = check_box_tag "with_user", 1, !params[:with_user].blank? 
        With user

      %label.checkbox 
        = check_box_tag "with_meta_data", 1, !params[:with_meta_data].blank? 
        With meta data

      = button_tag type: 'submit', class: 'btn btn-small' do
        Filter

  .span9
    %table.table.table-striped
      %thead
        %tr
          %th Last name
          %th First name
          %th Pseudonym
          %th.id Id
          %th User
          %th Meta data 
      %tbody
        =render @people



= paginate @people, theme: 'app_admin'

