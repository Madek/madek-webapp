%h1
  Transfer #{@person.meta_data.count} meta data from #{@person}

%p Transfer all meta data associated with this person to an other person.


= form_tag transfer_meta_data_app_admin_person_path(@person.id), method: 'POST', class: "form-horizontal" do

  .control-group
    %label.control-label Id of the receiving person:
    .controls
      = text_field nil, :id_receiver, type: 'number', id: "id_receiver"


  = button_tag type: 'submit', class: 'btn btn-warning' do
    Transfer

