:ruby
  element = {
    name: 'Preloaders', todo: true,
    cssSelector: '.ui-preloader',
    cssFile: '_ui-preloaders.scss',
    section: '7.8',
    mods: {
      nil => 'Default',
      'small' => 'Small variation',
      'dark' => 'Dark variation',
      'dark small' => 'Dark small variation'
    }
  }

= render_partial('element', element) do
  - element[:mods].each do |name, description|
    = render_partial('example', { mod: name}) do

      .ui-preloader{class: name}
        Loading in progress
