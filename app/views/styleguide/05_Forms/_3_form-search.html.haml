:ruby
  element = {
    name: 'Search input', todo: true,
    cssFile: '_ui-search-field.scss',
    cssSelector: '.ui-search',
    section: '5.3',
    mods: {
      nil => 'Default',
      'large' => 'Large variation'
    }
  }

= render_partial('element', element) do
  - element[:mods].each do |name, description|
    = render_partial('example', { mod: name}) do

      .ui-search{class: name}
        %input.block.ui-search-input{:type => "text"}/
        %button.primary-button.ui-search-button Search
