:ruby
  element = {
    name: 'Mini icons', todo: true,
    cssFile: '_icons.scss',
    cssSelector: '.icon-',
    section: '6.7',
    description: 'Destined to live within only tag cloud buttons, mini icons are small variations on some of the above icons that need to use another character and grid size to be perfectly crips.'
  }

= partial('element', element) do
  = partial('example') do

    = component('icon.group-mini')
    = component('icon', 'icon-user-mini')
    = component('icon', 'icon-tag-mini')
