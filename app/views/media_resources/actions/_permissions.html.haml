%li.ui-drop-item
  %a{:title => _("Read/edit permissions"), 
     :"data-open-permissions" => true, 
     :"data-media-resource-id" => media_resource.id,
     :"data-manageable" => current_user.authorized?(:manage, MediaResource.find(params[:id])).to_json,
     :"data-title" => media_resource.title}
    %i.icon-lock.mid
    = _("Permissions")
