- content_for :content do
  .bright.ui-container.pal.context-parents.rounded-right.rounded-bottom

    %h2.title-l.separated.mbl
      = n_("This entry is contained in one set", "This entry is contained in %d sets" % @parents.count, @parents.count)

    = render :partial => "media_resources/wrapper",
             :locals => {:media_resources => @parents,
                         :with_actions => true}

= render "media_entries/layout"
