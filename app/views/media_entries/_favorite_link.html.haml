= link_to toggle_favorites_media_entry_path(media_entry), :remote => true, :"data-type" => "html", :method => :post do
  - s = (current_user.favorite_ids.include?(media_entry.id) ? "on" : "off")
  %div{:class => "button_favorit_#{s}" }