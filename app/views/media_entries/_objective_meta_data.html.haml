%h3.title-l.separated.mbl
  = _("File")
%table.borderless
  %tbody
    - @objective_meta_data.each do |meta_datum|
      %tr
        %td.ui-summary-label= meta_datum.first
        %td.ui-summary-content= meta_datum.last
