- content_for(:header) do
  = component('page-header',
      icon: component('icon.pen'),
      text: t(:media_entry_meta_data_header_prefix) + @get.title,
      actions: nil)

- content_for(:overview) do
  = react('FormResourceMetaData', get: @get)

= render template: 'layouts/app_resource'
