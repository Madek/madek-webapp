:ruby
  get ||= nil
  fail 'No Presenter!' unless get.is_a?(Presenter)

  thumb = {
    mods: ['media-entry'],
    title: get.title,
    picture: get.image_url,
    href: get.url,
    # yes this is *inside* the thumb… workaround hacky CSS :/
    meta: {
      title: get.title
    },
    badge_top_left: deco('privacy-status-icon', get: get)
  }

= component('thumbnail', thumb)
