task_defaults:

  traits:
    rbenv: true
    ruby: true

  environment_variables:
    RAILS_ENV: test

  scripts:
    bundle:
      timeout: 500
      order: 1
      body: bundle install
    main:
      order: 2
